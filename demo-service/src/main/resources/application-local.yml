spring:
  datasource:
    url: jdbc:h2:mem:setlog;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    hikari:
      maximum-pool-size: 5
      connection-timeout: 1000
