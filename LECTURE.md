<!--
Generated from Bear.
Source kind: lecture
Bear note id: 2A434F79-0C81-44DD-A958-A54B18A6889E
Bear title: [복제] [멘토 특강] [유호균] SRE301: Grafana, Prometheus로 배우는 장애 모니터링 및 모의 대응 훈련
Bear content hash: e07328a
Bear content length: 75464
Bear modified: 2026-06-19T08:54:15Z
Synced at: 2026-06-19T08:54:19Z
Do not edit this file directly. Run `python3 scripts/sync-bear-sources.py`.
-->

# [복제] [멘토 특강] [유호균] SRE301: Grafana, Prometheus로 배우는 장애 모니터링 및 모의 대응 훈련
#6. 멘토링/0.소마/2026/특강#

**[이런 특강이에요]**
- [Grafana](https://grafana.com/), [Prometheus](https://prometheus.io/)를 처음 다뤄보는 분도 따라올 수 있도록 기본 개념부터 **실습 중심**으로 진행하는 워크샵입니다.
- SRE Golden Signals를 간단히 짚은 뒤 Latency, Traffic, Errors, Saturation 지표를 PromQL로 구성하고 Grafana Dashboard에서 관찰합니다.
- 이어서 모의 장애를 발생시키고, Grafana 지표와 Linux/Docker 도구로 증거를 확인한 뒤 1차 대응과 회복 검증까지 실습합니다.
- 서비스 지연, 에러 증가, 디스크 초과, CPU Saturation 상황을 통해 “무엇을 먼저 보고, 어떻게 판단해야 하는지”를 반복 훈련합니다.
- `systemctl`, `journalctl`, `df`, `du`, `top`/`htop`, `free`, `ss`, `ip`, `dig`, `dmesg`, `iostat`, `vmstat`, `lsof` 같은 기본 Linux 도구를 장애 대응 맥락에서 함께 다룹니다.

**[목표]**
- **[SRE Golden Signals](https://sre.google/sre-book/monitoring-distributed-systems/)**인 Latency, Traffic, Errors, Saturation의 개념을 이해한다.
- Grafana, Prometheus를 활용해 서비스 지표를 수집하고 시각화하는 기본 흐름을 익힌다.
- Prometheus를 활용해 Golden Signals뿐만 아니라 **본인 서비스에 필요한 도메인 세부 지표**를 설계할 수 있다.
- Grafana, Docker, EC2, Linux 기본 도구를 활용해 모의 장애 상황에서 지표 확인, 원인 가설 수립, 1차 초기 대응, 회복 검증, 회고를 수행할 수 있다.

**[이런 분이 특강을 들으면 좋겠어요]**
- Grafana, Prometheus를 처음 다뤄보는 분
- 서비스 모니터링을 어떻게 시작해야 할지 막막한 분
- 실제 장애 상황에서 무엇부터 확인해야 할지 알고 싶은 분
- Docker, EC2, Linux 환경에서 서비스 상태를 진단하는 기본기를 익히고 싶은 분
- 팀 프로젝트에 최소한의 모니터링과 장애 대응 체계를 적용해보고 싶은 분

**[3시간 진행 기준]**
- 0:00-0:45 개념 압축: Monitoring, Prometheus, Grafana, Golden Signals, Baseline
- 0:45-0:55 휴식
- 0:55-1:55 실습 1: Dashboard 읽기, Linux/Docker 진단, Incident 1~2
- 1:55-2:05 휴식
- 2:05-2:50 실습 2: Incident 3~4, 회복 검증, Follow-up Action Items
- 2:50-3:00 Mini Postmortem / Runbook 정리

**[목차]**
1. 장애 대응 관점에서 보는 Monitoring
2. Prometheus 기본 개념: Metrics, Scrape, Time Series, PromQL
3. Grafana 기본 개념: Dashboard, Panel, Time Range
4. SRE Golden Signals: Latency, Traffic, Errors, Saturation
5. Docker 기반 Prometheus, Grafana 실습 환경 구성
6. 서비스 정상 상태 Baseline 관찰하기
7. Prometheus로 서비스 지표와 도메인 지표 설계하기
8. Grafana Dashboard에서 장애 징후 읽기
9. Linux 기본 진단 도구 실습 (`systemctl`, `journalctl`, `df`, `du`, `htop`, `free`, `dig`, `netstat`, `dmesg`)
10. Incident 1: 서비스 지연 모의 대응
11. Incident 2: 에러 증가 모의 대응
12. Incident 3: 디스크 초과 모의 대응
13. Incident 4: CPU Saturation 모의 대응
14. 장애 대응 Runbook 및 Mini Postmortem 작성

참고 키워드: long tail/short head request, cpu affinity, oom killer, file descriptor leak, connection pool exhaustion, thundering herd

![](image%2019.png)

---
## TL;DR 요약
> 우리는 기대하는 수준까지 올라가는 것이 아니라 훈련한 수준까지 떨어진다.
- Docker, K8s, Kubernetes를 쓰더라도 장애 대응의 바닥에는 OS, Linux, Metric에 대한 도메인 지식이 있다.
- 장애 초반에는 ~~원인을 맞히는 것~~ 보다 **사용자 증상을 완화하고** **장애 영향 범위, 지속시간** 을빠르게 좁히는 편이 낫다.
- 증상 기반 page signal로 사람을 깨우고, diagnostic signal은 원인 후보를 좁히는 데 사용한다.
- 좋은 대응은 한 번에 하나만 바꾸고, 같은 지표와 같은 사용자 여정으로 회복을 확인하는 것이다.

## 오늘의 훈련 루틴
1. 사용자 증상을 확인한다.
2. Page signal을 확인한다.
3. Diagnostic signal로 가설을 좁힌다.
4. Linux/Docker 증거로 가설을 검증한다.
5. 사용자 영향을 줄이는 1차 완화 조치를 하나 실행한다.
6. 같은 page signal과 같은 사용자 여정으로 회복을 검증한다.
7. Follow-up Action Items로 재발 방지 대책을 남긴다.

![](image%2011.png)

---

## 0. 들어가면서
 `Trade off, Trade off, Trade off!`
앞으로 엔지니어 생활을 하면서 Trade off를 생각하세요.
AI 시대에도 성능과 안정성을 생각하신다면요.
- 구글 소프트웨어 엔지니어는 이렇게 일한다: Time, Scale, Trade-Offs
- SRE tradeoff: velocity, reliability -> SLO
- 알고리즘: 시간 Big(O), 공간 트레이드오프
- [Project management triangle](https://en.wikipedia.org/wiki/Project_management_triangle): Time, Performance, Cost
![](image%2017.png)
- CAP 이론
![](image%2018.png)<!-- {"width":580} -->


이 특강에서도 같은 방식으로 봅니다. 더 많은 도구를 외우는 것이 아니라, 시간과 영향 범위가 제한된 상황에서 어떤 신호를 먼저 보고 어떤 조치를 선택할지 훈련합니다.

---
## 1. 장애 대응 관점에서 보는 Monitoring
> [Google SRE monitoring distributed system - sre golden signals](https://sre.google/sre-book/monitoring-distributed-systems/#definitions)
장애: 사용자가 기대한 서비스 품질을 제공하지 못하거나, 제공하더라도 품질이 의미 있게 낮아진 상태입니다.
Monitoring: “실시간 정량 데이터를 수집·처리·집계·표시하는 것”
https://chatgpt.com/share/6a3230b3-7b08-83ee-b30c-dde9f9387563

[The USE Method — Brendan Gregg](https://www.brendangregg.com/usemethod.html)
장애 대응과 성능 분석의 기본 체크리스트입니다. Utilization, Saturation, Errors로 리소스를 보는 방식인데, 특히 “utilization이 낮아도 saturation이 있을 수 있다”는 점이 중요합니다. Brendan Gregg는 짧은 burst가 평균 지표에 묻혀도 saturation과 latency 문제를 만들 수 있다고 설명합니다.

### 1.1 장애 대응의 목표: 원인 규명보다 피해 축소
> [Claude Chat History](https://claude.ai/chat/0bb4fd8e-b776-4011-9568-9a96bc8e28a2)
![](image.png)
![](image%202.png)


![](image%203.png)
---
### 1.2 로그부터 보면 왜 늦어지는가

LTES:
Latency, Traffic, Error, Saturation![](image%205.png)
### 모니터링이 없을 때
1. 장애가 난다.
2. 사용자 신고나 에러 메시지를 본다.
3. 서버 로그와 프론트 로그를 뒤진다.
4. 재현을 시도한다.
5. 원인 후보가 너무 많다.
6. 늦게 완화한다.

### 모니터링이 있을 때
1. 장애가 난다.
2. 사용자 신고나 에러 메시지를 본다.
3. Latency, Traffic, Errors, Saturation 중 어떤 신호가 먼저 흔들렸는지 본다.
4. 정상 Baseline과 비교한다.
5. 영향 범위와 시작 시점을 좁힌다.
6. 로그와 Linux 명령어로 가설을 검증한다.
7. 완화 조치를 실행한다.
8. 다시 지표로 회복 여부를 확인한다.

**장애가 났을 때 로그부터 보는 사람과, 먼저 지표로 전장을 보는 사람은 복구 속도가 다릅니다.”**

### 1.3 Alert에 대해서
* Page (페이지, 페이저): 특정 사람의 주의를 긴급하고 적극적으로 끌려는 모든 것 (예: '삐삐삐' 소리를 내는 호출기나 휴대전화, ~~삐삐~~).
![](image%204.png)<!-- {"width":433} -->
* Rule (규칙): 어떤 모니터링 시스템이든 특정 관심 조건을 감지하기 위한 온갖 종류의 논리.
* Alert (알림, 알람): 사용자에게 전달하려고 만든 규칙의 구체적인 표현. 웹 페이지, 이메일, 문자, 전화, Slack, Discord 채널 메시지, 티켓 자동 생성 등이 그 예다.

### 1.4 ‘증상 (Symptoms)’ 기반 알림과 ‘원인 (Causes)’ 기반 알림
- 증상 (Symptoms) 기반 알림이란? ✅
  - 사용자가 네이버에서 검색했을 때 검색 쿼리 대기시간이 늘어져서 검색 결과를 늦게 받고 있음.
- 원인 (Causes) 기반 알림이란? 
  - 데이터베이스의 DB Connection Pool이 고갈되어 app의 db connection 획득이 늦어지고 있음.

## 알람
- [My Philosophy on Alerting](https://docs.google.com/document/d/199PqyG3UsyXlwieHaqbGiWVa8eMWi8zzAn0YfcApr8Q/edit?tab=t.0#heading=h.fs3knmjt7fjy)
- [번역: My Philosophy on Alerting](https://docs.google.com/document/d/1-Fnb2ibZ6fQJBA4az_7YHdzidYJHnDhTKcl-reGdu3A/edit?tab=t.0)
- [Sensitivity and specificity](https://en.wikipedia.org/wiki/Sensitivity_and_specificity)
- 원인이 아니라 증상에 알람을 걸어야 한다.
- 근접 원인: 데이터베이스 서버가 죽었다 -> 증상: 사용자 데이터가 이용 불가능한가
- 실전 메시지: `: Also firing: `
```
TooMany500StatusCodes
Served 10.7% 5xx results in the last 3 minutes!
Also firing:
	JanitorProcessNotKeepingUp
	UserDatabaseShardDown
	FreshnessIndexBehind
```

> 모니터링은 “문제를 자동으로 찾아주는 도구”가 아니라, 장애 상황에서 **탐색 공간을 줄이고 의사결정 순서를 강제하는 장치**다.

### 1.5 장애란?
> [Google SRE - Anatomy of an incident management handbook](https://sre.google/resources/practices-and-processes/anatomy-of-an-incident/) 
> SRE Book — Managing Incidents (Ch.14) — "When to Declare an Incident" 3대 기준
> SRE Workbook — Incident Response (Ch.9) — resolving vs managing 정의
> Incident Management at Google (Google Cloud Blog) — "declaring an incident"의 의미
> 1. **Anatomy of an Incident — Google's Approach to Incident Management** (handbook, 2022)
>    - 전체: [PDF](https://sre.google/static/pdf/Anatomy_Of_An_Incident.pdf)
>    - 정의: [§ What Is an Incident? (pp.2–3)](https://sre.google/static/pdf/Anatomy_Of_An_Incident.pdf#page=8)
>    - 구분: [§ Not Everything Is an Incident (p.4)](https://sre.google/static/pdf/Anatomy_Of_An_Incident.pdf#page=10)
> 2. **SRE Book — Managing Incidents (Ch.14)**
>    - 전체: [Managing Incidents](https://sre.google/sre-book/managing-incidents/)
>    - 3대 기준: [When to Declare an Incident](https://sre.google/sre-book/managing-incidents/#when-to-declare-an-incident)
> 3. **SRE Workbook — Incident Response (Ch.9)**
>    - 전체: [Incident Response](https://sre.google/workbook/incident-response/)
>    - resolving vs managing: [Incident Management at Google](https://sre.google/workbook/incident-response/#incident-management-at-google)
> 4. **Incident Management at Google (Google Cloud Blog)**
>    - 전체: [adventures in SRE-land](https://cloud.google.com/blog/products/gcp/incident-management-at-google-adventures-in-sre-land)
>    - "declaring an incident": [해당
  문장](https://cloud.google.com/blog/products/gcp/incident-management-at-google-adventures-in-sre-land#:~:text=What%20does%20declaring%20an%20incident%20mean)
용어 정의부터,
- 사전적 정의 ITIL (IT Infrastructure Library): IT 서비스를 비즈니스 목표에 맞게 계획, 제공, 운영 및 지속적으로 개선하기 위한 글로벌 표준 IT 서비스 관리(ITSM) 프레임워크
- 구글의 ITIL 자체적 정의: 
  - Outage: 서비스 이용 불가가 지속되는 기간(현상). 계획적이면 incident가 아니고, 예기치 못한 것이면 대부분 incident가 됨 -> `Sometimes an incident can be caused by an outage, which is a period of service unavailability.`
  - Incident: "에스컬레이션 + 즉각적 대응 + 조직적 대응"이 필요한 사건. outage는 그 원인 중 하나일 뿐임. -> `An incident is defined as an unplanned interruption or reduction in quality of an IT service (a service interruption).
- Incident를 선언할 3가지 기준
> [Google SRE - Incident Management: Key to Restore Operations](https://sre.google/sre-book/managing-incidents/#when-to-declare-an-incident)
1. 문제 해결에 두 번째 팀을 참여시켜야 하는가?
   1. Outage가 고객에게 보이는가?
   2. 한 시간 동안 집중 분석했는데도 문제가 해결되지 않았는가?

Incident management란? 
incident로 인한 disruption을 제한하고 정상 business operations를 가능한 빨리 복구하기 위한 것

### 1.6 장애 대응
> [LINE의 장애 보고와 후속 절차 문화](https://engineering.linecorp.com/ko/blog/line-failure-reporting-and-follow-up-process-culture)
![](image%206.png)
> <병풍친다>

장애 대응 초반의 목표는 root cause를 맞히는 것이 아닙니다.
먼저 봐야 할 것은 세 가지입니다.
1. 사용자가 얼마나 영향을 받고 있는가?
2. 지금 문제가 더 나빠지고 있는가?
3. 어떤 완화 조치를 먼저 해야 피해를 줄일 수 있는가?
⠀
원인 분석은 중요합니다.
하지만 사용자가 깨져 있는 동안 “정답”을 찾고 있으면 이미 늦습니다.

장애 대응 초반에는 **원인 규명보다 피해 축소가 먼저**입니다.
Root cause는 포스트모템에서 다뤄도 됩니다.
하지만 사용자 영향은 지금 줄여야 합니다.

문제는 여기서 인간이 자주 틀린다는 점입니다.
실무에서 장애를 겪어보면, 완화 조치라고 생각한 행동이 오히려 장애를 키우는 경우가 많습니다.
대표적인 예가 **롤백해야 할 상황에서 추가 배포를 하는 것**입니다.

예를 들어 장애 원인을 더 잘 보기 위해 로깅을 추가해서 다시 배포하고 싶어집니다.
겉으로는 합리적으로 보입니다.
원인을 더 잘 알기 위한 행동처럼 보이기 때문입니다.

하지만 장애 중 추가 배포는 새로운 변수를 만듭니다.
기존 문제가 배포 때문인지, 설정 때문인지, 트래픽 때문인지도 아직 모르는 상태에서
새 코드, 새 설정, 새 로그, 새 빌드, 새 배포 타이밍이 추가됩니다.

그러면 상태공간이 더 커집니다.
복구는 더 어려워지고, 원인 파악도 더 흐려집니다.
장애 대응에서 좋은 완화 조치는 대개 문제를 “더 많이 알게 해주는 조치”가 아니라,
**문제를 더 작게 만들고 되돌리기 쉽게 만드는 조치**입니다.

그래서 장애 초반에는 다음 원칙을 가져야 합니다.
* 새 변수를 늘리지 않는다.
* 한 번에 하나만 바꾼다.
* 되돌릴 수 있는 조치를 우선한다.
* 사용자 영향이 큰 경우 fix-forward보다 rollback을 먼저 검토한다.
* 조치 후에는 반드시 지표로 회복 여부를 확인한다.

롤백은 패배가 아닙니다.
롤백은 탐색 공간을 줄이는 가장 강력한 완화 조치입니다.

장애 대응에서 가장 위험한 사람은 아무것도 모르는 사람이 아니라,
조금 아는 상태에서 확신을 가지고 여러 가지를 동시에 바꾸는 사람입니다.

장애 대응은 영웅적으로 고치는 일이 아닙니다.
불확실한 상황에서 피해를 줄이고, 변경을 통제하고, 회복을 검증하는 일입니다.

---

### Brendan Gregg's USE Method
> https://www.brendangregg.com/usemethod.html
![](image%2023.png)
- **활용률(utilization)** = 리소스가 “일하고 있는 비율”
- **포화도(saturation)** = 리소스가 처리하지 못해서 “대기열/지연이 생긴 정도”
- **오류(errors)** = 실패·재시도·장치 에러

### 1.6 다시 장애 -> 알람 -> 대응 -> 모니터링
> [Google SRE monitoring distributed system - sre golden signals](https://sre.google/sre-book/monitoring-distributed-systems/#monitoring) -> 모니터링 정의

- 모니터링 정의: 시스템의 실시간 정량 데이터를 수집·처리·집계·표시 (`Collecting, processing, aggregating, and displaying real-time quantitative data about a system, such as query counts and types, error counts and types, processing times, and server lifetimes.`)

> 응급실 간호사 Triage 설문 문항
1. 언제부터 이상했나?
2. 누가 / 어디가 / 얼마나 영향받고 있나? (영향도 범위 Scope , 심각도 Severity 진단) Triage
3. 어떤 신호가 먼저 흔들렸나?
4. 조치 후 회복됐나?

---

## 2. 모니터링과 Prometheus 기본 개념
이 장의 목표는 Prometheus를 깊게 외우는 것이 아니라, 실습에서 Grafana 패널과 PromQL을 읽을 수 있을 만큼의 최소 언어를 맞추는 것입니다.

### 2.1 White-box Monitoring, Black-box Monitoring
> [Google SRE monitoring distributed system - sre golden signals](https://sre.google/sre-book/monitoring-distributed-systems/)

- White-box monitoring: 시스템 내부가 노출하는 메트릭, 로그, 런타임 통계, `/actuator/prometheus` 같은 엔드포인트를 기반으로 관찰합니다.
- Black-box monitoring: 사용자가 보는 것처럼 외부에서 서비스 동작을 확인합니다. HTTP 요청이 성공하는지, 응답이 느리지 않은지, 결과물이 만들어지는지 확인합니다.

![](image%208.png)
![](image%209.png)
![](image%2010.png)

장애 대응에서는 둘 중 하나만 보지 않습니다. 예를 들어 사용자가 “업로드가 오래 걸린다”고 느끼는 것은 black-box 증상이고, `hikaricp_connections_pending`이 올라가는 것은 white-box diagnostic signal입니다.

> **JMH(Java Microbenchmark Harness)** -> [openjdk/jmh: https://openjdk.org/projects/code-tools/jmh](https://github.com/openjdk/jmh)

Postman 같은 도구로도 Avg response time, p90, p95, p99, Error rate를 볼 수 있습니다. 오늘은 같은 질문을 Prometheus와 Grafana로 지속적으로 관찰합니다.
![](image%207.png)

### 2.2 Push vs Pull Architecture
Prometheus는 기본적으로 pull model입니다. Prometheus가 정해진 주기마다 target의 metrics endpoint를 scrape합니다.

> 그림: Prometheus 공식 아키텍처. Prometheus server가 target하exporter를 pull scrape하고, Grafana와 Alertmanager가 같은 시계열 데이터를 소비한다.
![](sre301-prometheus-architecture.png)<!-- {"width":700} -->

실습에서 중요한 질문은 이것입니다.
- Prometheus가 setlog를 scrape하고 있는가?
- exporter target이 살아 있는가?
- target down은 사용자 증상인가, 원인 후보를 좁히는 diagnostic signal인가?

### 2.3 Prometheus에서 보는 네 가지 단위
- Metrics: 관찰하고 싶은 숫자입니다. 예: 요청 수, latency bucket, render job 수, queue depth
- Scrape: Prometheus가 target에서 metrics를 가져오는 동작입니다.
- Time Series: metric name과 label 조합으로 만들어지는 시계열입니다.
- PromQL: 시계열을 질문하는 언어입니다.

실습에서 주로 볼 metric type은 세 가지입니다.
- Counter: 계속 증가하는 값입니다. 요청 수, 에러 수, render job 수에 사용합니다.
- Gauge: 오르내리는 값입니다. queue depth, memory usage, pending connection에 사용합니다.
- Histogram: latency처럼 분포가 중요한 값에 사용합니다. p95, p99 계산에 사용합니다.

실습에서 반복해서 쓸 PromQL 패턴입니다.

```promql
rate(http_server_requests_seconds_count[5m])
```

```promql
sum(rate(http_server_requests_seconds_count{status=~"5.."}[5m]))
/
clamp_min(sum(rate(http_server_requests_seconds_count[5m])), 0.001)
```

```promql
histogram_quantile(
  0.95,
  sum(rate(http_server_requests_seconds_bucket[5m])) by (le, uri)
)
```

`rate`는 Counter의 증가 속도를 보고, `histogram_quantile`은 Histogram bucket에서 p95 같은 분위수를 계산하고, `by`는 API나 status 같은 기준으로 묶어서 봅니다.

### 2.4 시계열 데이터베이스로 보는 이유
그냥 RDB에다가 넣으면 안 되나?

네이버에는 2009년부터 유지되어온 모니터링 서비스가 있었습니다. 이름은 공교롭게도 Palantir, 《반지의 제왕》에 등장하는 모든 것을 멀리서도 꿰뚫어 보는 천리안의 수정구슬입니다.

문제는 Alter 작업입니다. 색인이 걸려 있는 수십억 개의 시계열 데이터에서 schema 변경이나 고카디널리티 조회가 반복되면 DB 부하가 크게 올라갈 수 있습니다.
![](image%2012.png)

- 샤딩
![](image%2026.png)

MongoDB는 Schema가 존재하지 않기 때문에 column 추가가 자유롭고, 시계열 압축도 지원합니다.
https://www.mongodb.com/ko-kr/docs/manual/core/timeseries/timeseries-compression/

하지만 오늘의 결론은 “어떤 저장소가 정답인가”가 아닙니다. 장애 대응 도구는 Reliability와 Accuracy 사이에서 목적이 다릅니다.

### 2.5 Prometheus is not the only answer
> https://prometheus.io/docs/introduction/overview/

Reliability: `장애 중에도 관찰 가능성을 유지하는 운영 신뢰성`
-> “장애가 나도 Prometheus 자체는 최대한 살아 있고, 지금 수집된 지표를 볼 수 있어야 한다.”
![](image%2013.png)

Accuracy: `데이터의 회계적 완전성`
-> “모든 요청이 빠짐없이 기록되고, 중복 없이 정산되며, 나중에 감사 가능해야 한다.”
![](image%2014.png)

[네이버 검색의 대규모 메트릭 저장소, VictoriaMetrics 운영기](https://d2.naver.com/helloworld/6475419)는 이런 trade-off를 실제 운영 사례로 볼 수 있는 참고자료입니다.

### CAP Theorem
C: Consistency (일관성) -> 모든 구성 요소는 같은 데이터를 가져야 한다.
A: Availability (가용성) -> 개별 구성 요소는 항상 읽고 쓸 수 있어야 한다.
P: Partition Tolerance (분할 내성) -> 시스템은 물리적 네트워크 격리 상황에서도 동작해야 한다.
![](image%2016.png)
![](image%2015.png)

## 3. Grafana 기본 개념: Dashboard, Panel, Time Range
Grafana는 “숫자를 예쁘게 보는 도구”가 아니라, 장애 상황에서 같은 질문을 빠르게 반복하게 해주는 화면입니다.

### Dashboard
여러 panel을 묶은 하나의 관찰 화면입니다. 오늘은 `SRE301 Golden Signals Lab` 대시보드에서 시작합니다.

> 그림: Grafana 공식 Dashboard UI. 실습 중에는 time range, refresh, panel, legend 위치를 먼저 확인한다.
![](sre301-grafana-dashboard-ui.png)<!-- {"width":700} -->

### Panel
하나의 질문을 시각화한 단위입니다. 예: “p95 latency가 올랐는가?”, “5xx ratio가 threshold를 넘었는가?”, “render queue가 밀리는가?”
![](image%2020.png)

### Time Range
장애 대응에서는 시간 범위가 중요합니다. 실습에서는 먼저 최근 15분을 보고, 필요하면 장애 시작 전후로 좁힙니다.
![](image%2021.png)

## 4. SRE Golden Signals: Latency, Traffic, Errors, Saturation
> [Google SRE monitoring distributed system - sre golden signals](https://sre.google/sre-book/monitoring-distributed-systems/#xref_monitoring_golden-signals)

- Latency: 사용자가 얼마나 기다리는가?
- Traffic: 요청이 얼마나 들어오는가?
- Errors: 사용자가 실패를 보는가?
- Saturation: 시스템이 얼마나 여유 없이 밀리고 있는가?

### 4.1 Page Signal과 Diagnostic Signal
| 구분 | 의미 | 예시 |
|---|---|---|
| Page signal | 사람을 깨워야 하는 사용자-visible 증상 | p95 latency 상승, 5xx ratio 상승, render failure ratio 상승, render backlog |
| Diagnostic signal | 원인 후보를 좁히는 보조 신호 | DB pool pending, target down, debug log bytes, CPU usage |

> 그림: Page signal은 사용자 증상에, Diagnostic signal은 원인 후보 축소에 사용한다.
![](sre301-signals-map.png)<!-- {"width":760} -->

CPU, DB, disk는 중요한 신호지만 그 자체가 항상 page signal은 아닙니다. 사용자가 기다리는지, 실패하는지, 작업이 밀리는지를 먼저 봅니다.

### 4.2 실습에서 쓸 PromQL 패턴
PromQL은 “metric을 고르고, label로 좁히고, 시간 범위를 잡고, function으로 계산하고, operator로 묶거나 나누는 언어”입니다.
실습에서는 문법 전체보다 아래 패턴을 반복해서 읽고 고치는 것이 중요합니다.

> 그림: PromQL은 selector에서 시작해 range, function, aggregation, ratio/threshold 순서로 읽는다.
![](sre301-promql-pipeline.png)<!-- {"width":760} -->

#### Basics
- Metric 이름: `http_server_requests_seconds_count`, `setlog_vlog_render_jobs_total`처럼 관찰할 숫자의 이름입니다.
- Label filter: `{job="setlog", uri=~"/api/.*", status=~"5.."}`처럼 시계열을 좁힙니다.
- Label matcher:
  - `=`: 정확히 일치
  - `!=`: 정확히 일치하지 않음
  - `=~`: 정규식 일치
  - `!~`: 정규식 불일치
- Instant vector: `hikaricp_connections_pending{job="setlog"}`처럼 지금 시점의 값을 봅니다.
- Range vector: `http_server_requests_seconds_count[5m]`처럼 최근 5분 구간을 봅니다. `rate`, `increase` 같은 함수는 보통 range vector를 입력으로 받습니다.
- Counter는 계속 증가하므로 원값을 그대로 보지 말고 `rate(counter[5m])`로 증가 속도를 봅니다.
- Gauge는 현재 상태 값이므로 `queue depth`, `pending connection`, `debug log bytes`처럼 원값을 직접 봐도 됩니다.
- Histogram은 `_bucket`과 `le` label을 사용해 `histogram_quantile`로 p95/p99를 계산합니다.
- PromQL은 안쪽부터 읽습니다. `sum(rate(metric[5m])) by (uri)`는 “최근 5분 증가율을 구한 뒤 uri별로 합친다”입니다.

#### Functions
- `rate(counter[5m])`: Counter가 초당 평균 얼마나 증가했는지 봅니다. 요청량, 에러량, job 처리량에 사용합니다.
- `increase(counter[5m])`: 최근 5분 동안 총 몇 번 증가했는지 봅니다. 짧은 실습 검증에서는 count 관점으로 읽기 쉽습니다.
- `histogram_quantile(0.95, ...)`: Histogram bucket으로 p95 latency를 계산합니다.
- `clamp_min(value, 0.001)`: 분모가 0에 가까울 때 비율 계산이 `Inf`나 `NaN`으로 튀는 것을 막습니다.

#### Operators
- 산술 operator: `/`, `*`, `+`, `-`로 비율이나 파생 값을 만듭니다. 예: 에러 요청 rate / 전체 요청 rate.
- 비교 operator: `>`, `<`, `>=`, `<=`로 alert threshold 조건을 표현합니다. 예: 5xx ratio `> 0.05`.
- Aggregation operator: `sum`, `avg`, `max`처럼 여러 time series를 합계, 평균, 최댓값으로 묶습니다. 실습에서는 대부분 `sum(...) by (uri)`, `sum(...) by (le, uri)`를 씁니다.
- `by`: 지정한 label만 남기고 묶습니다. 예: `by (uri)`는 API별로 본다는 뜻입니다.
- `without`: 지정한 label만 제거하고 묶습니다. 실습에서는 `by`를 먼저 씁니다.
- Set operator: `and`, `or`, `unless`는 조건을 조합할 때 쓰지만, 이번 실습의 기본 쿼리에서는 거의 쓰지 않습니다.
- Vector matching: `on(...)`, `ignoring(...)`은 서로 다른 label set을 가진 벡터를 나눌 때 씁니다. 처음에는 분자와 분모의 label을 일부러 같게 만들어 복잡한 matching을 피합니다.

#### 실습에서 반복해서 쓸 쿼리
Traffic:
```promql
sum(rate(http_server_requests_seconds_count{job="setlog", uri=~"/api/.*"}[5m]))
```

5xx ratio:
```promql
sum(rate(http_server_requests_seconds_count{job="setlog", uri=~"/api/.*", status=~"5.."}[5m]))
/
clamp_min(sum(rate(http_server_requests_seconds_count{job="setlog", uri=~"/api/.*"}[5m])), 0.001)
```

API별 p95 latency:
```promql
histogram_quantile(
  0.95,
  sum(rate(http_server_requests_seconds_bucket{job="setlog", uri=~"/api/.*"}[5m])) by (le, uri)
)
```

Render failure ratio:
```promql
sum(rate(setlog_vlog_render_jobs_total{job="setlog", result="failure"}[5m]))
/
clamp_min(sum(rate(setlog_vlog_render_jobs_total{job="setlog"}[5m])), 0.001)
```

CPU 사용률:
```promql
1 - avg(rate(node_cpu_seconds_total{mode="idle"}[2m]))
```

### 5.3 SRE Golden Signal
- Traffic:
  - 시스템이 처리하는 요청량
  - 예: 초당 HTTP 요청 수, RPS, QPS
  - PromQL 예:
    `sum(rate(http_server_requests_seconds_count[5m]))`
- Latency:
  - 요청을 처리하는 데 걸린 시간
  - 예: p95/p99 응답 시간
  - PromQL 예:
    `histogram_quantile(0.95, sum by (le) (rate(http_server_requests_seconds_bucket[5m])))`
- Errors:
  - 실패한 요청 비율 또는 개수
  - 예: HTTP 5xx 비율
  - PromQL 예:
    `sum(rate(http_server_requests_seconds_count{status=~"5.."}[5m])) / sum(rate(http_server_requests_seconds_count[5m]))`
- Saturation:
  - 시스템이 얼마나 “꽉 차서 대기”하고 있는가
  - 예: CPU run queue, load average / CPU core 수, connection pool 대기, disk I/O queue
  - PromQL 예:
    `node_load1 / count without (cpu, mode) (node_cpu_seconds_total{mode="idle"})`
  - `$ uptime`

## 5. Docker 기반 Prometheus, Grafana 실습 환경 구성
실습 환경의 목표는 완벽한 운영 환경을 만드는 것이 아니라, 장애를 만들고 관찰하고 완화하는 루프를 로컬에서 반복할 수 있게 하는 것입니다.

확인할 것:
- `docker compose ps`에서 필요한 container가 떠 있는가?
- Prometheus target이 `up`인가?
- Grafana Dashboard에서 baseline traffic이 보이는가?
- Incident 시작 전 정상 상태의 latency, error rate, queue depth를 한 번 기록했는가?

## 6. 서비스 정상 상태 Baseline 관찰하기
> 장애 대응은 “이상한 값”을 찾는 일이 아니라,
> **정상일 때의 값과 비교해서 무엇이 얼마나 달라졌는지 판단하는 일**

평상시 상태를 모르면 정상 상황이라고 판단하는 것이 불가능함.
1. 평소에는 얼마나 빠른가?
2. 평소에는 요청이 얼마나 들어오는가?
3. 평소에는 에러가 얼마나 나는가?
4. 평소에는 CPU, 메모리, 디스크, 커넥션 풀이 어느 정도 여유가 있는가?

정상 상태라면 기준 (Baseline)을 모르는 것이기에 장애 판단이 어려움.
- CPU 70%는 높은가?
- p95 latency 300ms는 나쁜가?
- 5xx 0.5%는 장애인가?
- 메모리 80% 사용은 위험한가?

아래는 작은 서비스가 세울만한 기준 (Baseline)
```
정상 요청량: 20~30 RPS
p50 latency: 30ms
p95 latency: 120ms
p99 latency: 300ms
5xx error rate: 0~0.1%
CPU usage: 20~40%
Memory usage: 300MB / 512MB
DB connection active: 2~4개
Open file descriptor: 50~80개
```

기준 (Baseline)이 이렇다면,
장애 상황에서 아래와 같이 판단할 수 있음.
```
평소 p95 latency가 120ms였는데 지금 900ms다.
평소 5xx가 거의 없었는데 지금 4%다.
평소 DB connection pending이 0이었는데 지금 30개다.
평소 CPU는 40%였는데 지금도 45%다.
그러면 CPU보다는 DB connection pool saturation을 먼저 의심한다.
```

## 7. Prometheus로 서비스 지표와 도메인 지표 설계하기
> [setlog 앱 - App Store](https://apps.apple.com/kr/app/setlog/id6587576438)
- `SetLog`는 “친구들과 방을 만들고, 한 시간마다 2초씩 찍으면 하루치 영상을 브이로그처럼 만들 수 있는 앱”

즉 SetLog의 핵심 경험은 단순 카메라 앱이 아님
```
찍는다
→ 친구들과 같은 방에 모인다
→ 하루 동안 조각 영상이 쌓인다
→ 자동으로 하나의 브이로그가 된다
→ 저장하거나 공유한다
→ 공유된 내용을 바탕으로 채팅한다
```

그래서 SetLog의 도메인 지표는 단순히 “API가 200을 반환했는가?”가 아니라 다음 질문에 답해야 합니다.
```
사용자가 제때 찍을 수 있었는가?
친구 방이 정상적으로 동기화되었는가?
영상 조각이 유실되지 않았는가?
하루치 브이로그가 정상적으로 생성되었는가?
공유 가능한 결과물이 만들어졌는가?
채팅이 올바른 때에 도달하는가?
```

여기서 측정할 수 있는 지표는 다음과 같다.
|  **구분**  |  **질문**  |  **예시**  | 
|---|---|---|
|  서비스 지표  |  시스템이 정상인가?  |  API latency, 5xx, CPU, memory, DB pool  |
|  도메인 지표  |  제품 경험이 정상인가?  |  촬영 성공률, 방 동기화 지연, 영상 업로드 성공률, 브이로그 생성 성공률  |
|  비즈니스 지표  |  사업이 성장하는가?  |  가입자, 활성 사용자, 공유 수, 리텐션  |
|  분석 지표  |  사용자가 어떻게 행동하는가?  |  funnel, cohort, feature adoption  |

그리고 Prometheus가 위에서 Prometheus가 잘 하는 부분은 2가지이다.
- 서비스 지표
- 도메인 지표

지표로 삼지 말아야 할 것,
[Metric and label naming](https://prometheus.io/docs/practices/naming/)

특히 label에 `user_id`, `email`, `request_id` 같은 unbounded value를 넣지 말라.
시계열에는 항상 [Cardinality](https://itholic.github.io/database-cardinality/) 문제를 유의할 것
![](image%2024.png)<!-- {"width":606} -->![](image%2025.png)

### 7.11 실습 과제
1. SetLog 같은 앱을 하나 골라라.
2. 핵심 사용자 여정을 5단계로 쪼개라.
3. 각 단계마다 깨졌을 때 사용자 증상을 적어라.
4. 그 증상을 감지할 Prometheus metric을 설계하라.
5. Counter / Gauge / Histogram 중 하나를 고르라.
6. label을 설계하라.
7. cardinality 위험 label을 제거하라.
8. 마지막으로 구체적인 Golden Signal을 선택하고 PromQL을 2개 작성하라.

### 7.12 SetLog 예시 답안
```markdown
# SetLog Metrics Design

## Core Journey
1. 방 만들기
2. 2초 클립 캡쳐
3. 클립 업로드
4. 친구 동기화
5. daily vlog 렌더링

## User-visible Failures
- 방 생성 실패
- 찍은 영상 업로드 실패
- 친구 방에 늦게 보임
- 하루치 브이로그 생성 실패
- 공유 실패

## Metrics
- setlog_video_uploads_total{result, reason, network_type}
- setlog_video_upload_duration_seconds{network_type}
- setlog_room_sync_duration_seconds
- setlog_vlog_render_jobs_total{result, reason}
- setlog_vlog_render_queue_depth

## SLO
- 99% of uploads succeed within 10s
- 95% of uploaded clips appear in room within 3s
- 99% of render jobs complete within 2min

## Labels Removed
- user_id
- room_id
- video_id
- request_id
```

## 8. Grafana Dashboard에서 장애 징후 읽기
장애 상황에서 Grafana를 볼 때 목표는 “원인을 바로 맞히는 것”이 아니라 탐색 공간을 줄이는 것입니다.

먼저 사용자-visible 증상을 나타내는 page signal을 봅니다.
- 사용자가 기다리는가? -> `Latency p95`
- 사용자가 실패를 보는가? -> `Traffic and Status`, 5xx ratio, render failure ratio
- 작업이 밀리는가? -> `Vlog Render Queue`, render queue depth

그다음 diagnostic signal로 원인 후보를 좁힙니다.
- DB pool pending -> `DB Pool Saturation`
- Prometheus target up/down -> dependency 상태
- debug log bytes -> render debug log / disk 관련 가설
- CPU saturation -> `Node CPU Saturation`

CPU, DB, disk는 원인 후보일 수 있지만 그 자체가 항상 page signal은 아닙니다. 먼저 사용자 증상을 고정하고, 그 증상을 가장 직접적으로 설명하는 panel부터 봅니다.

실습에서 Dashboard를 볼 때마다 아래 순서로 표를 채웁니다.

| 순서 | 질문 | 출력물 |
|---|---|---|
| 1 | 사용자는 무엇을 겪는가? | 사용자 증상 |
| 2 | 어떤 page signal이 흔들렸는가? | Alert / Page Signal |
| 3 | 어느 API나 여정이 영향받는가? | 영향 범위와 심각도 |
| 4 | 어떤 diagnostic signal이 같은 시점에 움직였는가? | Diagnostic 신호 |
| 5 | Linux/Docker에서 같은 방향의 증거가 있는가? | Evidence |

금지할 습관:
- CPU 그래프가 높다는 이유만으로 CPU 장애라고 단정하지 않는다.
- DB exporter가 down이라는 이유만으로 바로 page하지 않는다.
- 로그부터 끝까지 뒤지기 전에 page signal과 시작 시점을 먼저 좁힌다.
- cleanup, restart, dependency 복구를 한 번에 섞지 않는다.

## 9. Linux 기본 진단 도구 실습 (`systemctl`, `journalctl`, `df`, `du`, `htop`, `free`, `dig`, `netstat`, `dmesg`)

> 그림: 사용자 증상과 Grafana 지표에서 시작해 필요한 Linux/Docker 명령만 좁혀 실행한다.
![](sre301-linux-diagnostic-map.png)<!-- {"width":760} -->
- `systemctl`
- `journalctl`
- `df` (display free disk space)
- `du` (display disk usage statistics)
- `htop`
- `free` (memory allocation)
- `dig` (DNS lookup utility)
- `netstat` (show network status)
- `dmesg` (display the system message buffer)
- `iostat` (report I/O statistics)
- `vmstat`
- `lsof` (list open files)
- `ss`
---
장애 진단에서는 명령어를 외우는 것이 아니라, **지표로 세운 가설을 OS에서 검증하는 것**이 중요하다.

### **9.1 서비스 프로세스가 살아 있는가?**
```
systemctl status myapp
systemctl is-active myapp
systemctl is-enabled myapp
systemctl list-units --failed
```

확인할 것:
```
active인가?
failed 상태인가?
재시작 루프가 있는가?
부팅 후 자동 시작되는가?	
```

주의:
```
active라고 해서 서비스가 정상이라는 뜻은 아니다.
프로세스는 살아 있어도 API는 500을 반환할 수 있다.
```

### **9.2 최근 서비스 로그에 에러가 있는가?**
```
journalctl -u myapp --since "10 minutes ago"
journalctl -u myapp -f
journalctl -u myapp -p warning..alert --since "1 hour ago"
journalctl -u myapp -b
journalctl -u myapp -b -1
```

확인할 것:
```
최근 에러가 있는가?
재시작 직전 로그가 있는가?
이전 부팅에서 죽은 흔적이 있는가?
```

주의:
```
systemctl status는 최근 로그 일부만 보여준다.
장애 분석은 journalctl로 시간 범위를 잡고 봐야 한다.
```

### **9.3 디스크가 가득 찼는가?**
```
df -h
df -ih
df -hT
```

확인할 것:
```
디스크 사용률이 100%에 가까운가?
inode가 고갈됐는가?
어느 mount point가 찼는가?
```

해석:
```
df -h  = 디스크 용량 확인
df -ih = inode 사용량 확인
```

주의:
```
용량은 남았는데 inode가 100%면 새 파일 생성이 실패할 수 있다.
```

### **9.4 무엇이 디스크를 먹고 있는가?**
```
du -xhd1 / | sort -h
du -xhd1 /var | sort -h
du -xhd1 /var/log | sort -h
du -sh .[!.]* * 2>/dev/null | sort -h
```

확인할 것:
```
큰 디렉터리가 어디인가?
로그가 쌓였는가?
Docker, cache, build artifact가 쌓였는가?
숨김 디렉터리가 큰가?
```

주의:
```
du -sh * 는 .cache, .docker 같은 숨김 디렉터리를 놓칠 수 있다.
```

### **9.5 df는 찼는데 du로 큰 파일이 안 보이는가?**
```
lsof | grep deleted
```

확인할 것:
```
삭제된 파일을 프로세스가 계속 잡고 있는가?
```

해석:
```
파일을 rm으로 지워도 프로세스가 file descriptor를 잡고 있으면 디스크 공간은 바로 해제되지 않는다.
```

대응:
```
systemctl restart myapp
```

주의:
```
재시작은 완화 조치다.
재시작 전 로그와 상태를 먼저 확보하는 것이 좋다.
```

### **9.6 메모리가 부족한가?**
```
free -h
free -h -w
vmstat 1
```

확인할 것:
```
available memory가 부족한가?
swap in/out이 발생하는가?
메모리가 계속 줄어드는가?
```

해석:
```
free가 낮다 ≠ 메모리 부족
available이 낮다 = 실제로 위험할 수 있음
```

`vmstat`에서 볼 것:
```
si = swap in
so = swap out
wa = I/O wait
r  = CPU run queue
b  = blocked process
```

주의:
```
Linux는 남는 메모리를 cache로 적극 사용한다.
free보다 available을 봐야 한다.
```

### **9.7 CPU가 바쁜가, 어디선가 hang 상태인가?**
```
htop
vmstat 1
```

확인할 것:
```
CPU 사용률이 높은가?
load average가 높은가?
run queue가 쌓이는가?
I/O wait가 높은가?
특정 프로세스가 CPU를 독점하는가?
```

`htop`에서 볼 것:
```
CPU%
MEM%
프로세스 상태 R (Running / Runnable)/S (Interruptible Sleep)/D (Uninterruptible Sleep)/Z (Zombie / Defunct)
Load average
Thread 수
```

주의:
```
CPU가 낮아도 서비스는 느릴 수 있다.
DB connection pool, disk I/O, network, external API 대기 때문일 수 있다.
```

### **9.8 디스크 I/O가 병목인가?**
```
iostat -xz 1
```

확인할 것:
```
%util이 높은가?
await가 높은가?
read/write가 몰리는가?
```

해석:
```
%util  = 디스크 장치가 바쁜 정도
await  = I/O 요청이 평균적으로 기다린 시간
```

주의:
```
CPU는 낮은데 latency가 높다면 disk I/O wait를 의심할 수 있다.
```

### **9.9 포트가 열려 있는가?**
```
ss -tulpen
ss -ltnp
```

또는 구형 환경:
```
netstat -tulnp
```

확인할 것:
```
앱이 기대한 포트에서 LISTEN 중인가?
어떤 프로세스가 포트를 잡고 있는가?
IPv4/IPv6 중 어디에 바인딩됐는가?
```

주의:
```
LISTEN은 포트가 열렸다는 뜻이지, API가 정상이라는 뜻은 아니다.
```

### **9.10 연결이 쌓이고 있는가?**
```
ss -ant
ss -s
ss -ant | awk '{print $1}' | sort | uniq -c | sort -nr
```

또는:
```
netstat -ant | awk '{print $6}' | sort | uniq -c | sort -nr
```

확인할 것:
```
ESTABLISHED가 과도하게 많은가?
TIME_WAIT가 급증하는가?
CLOSE_WAIT가 쌓이는가?
SYN_SENT가 쌓이는가?
```

해석:
```
ESTABLISHED = 연결 중
TIME_WAIT   = 짧은 연결이 많이 생성/종료됨
CLOSE_WAIT  = 상대는 닫았는데 내 앱이 close를 안 함
SYN_SENT    = 외부 연결 시도 중 응답 없음
```

주의:
```
CLOSE_WAIT가 많이 쌓이면 애플리케이션의 connection leak 가능성이 있다.
```

### **9.11 DNS가 문제인가?**
```
dig example.com
dig example.com A
dig example.com AAAA
dig +short example.com
dig @8.8.8.8 example.com
dig +trace example.com
```

확인할 것:
```
DNS 응답이 있는가?
A/AAAA 레코드가 올바른가?
내 resolver와 외부 resolver 결과가 다른가?
TTL이 긴가?
```

주의:
```
DNS가 성공해도 HTTP가 성공한다는 뜻은 아니다.
DNS는 HTTP 이전 단계만 확인한다.
```

### **9.12 앱 로그 밖에서 커널이 죽였는가?**
```
dmesg -T | tail -100
dmesg -T | grep -i -E "oom|killed|segfault|error|fail"
journalctl -k --since "30 minutes ago"
```

확인할 것:
```
OOM killer가 프로세스를 죽였는가?
segfault가 있었는가?
disk I/O error가 있었는가?
kernel/network driver 문제가 있었는가?
```

주의:
```
앱 로그에 에러가 없어도 커널이 프로세스를 죽였을 수 있다.
```

## **장애 상황별 빠른 명령어 세트**

### **서비스가 안 뜬다**
```
systemctl status myapp
journalctl -u myapp --since "10 minutes ago"
systemctl list-units --failed
```

### **API가 느리다**
```
htop
vmstat 1
ss -s
journalctl -u myapp --since "10 minutes ago"
```

### **5xx가 늘어난다**
```
journalctl -u myapp --since "10 minutes ago"
ss -ant | awk '{print $1}' | sort | uniq -c | sort -nr
dmesg -T | grep -i -E "oom|killed|error"
```

### **디스크가 찼다**
```
df -h
df -ih
du -xhd1 / | sort -h
du -xhd1 /var | sort -h
lsof | grep deleted
```

### **메모리가 의심된다**
```
free -h
vmstat 1
dmesg -T | grep -i -E "oom|killed"
```

### **네트워크/DNS가 의심된다**
```
ss -tulpen
ss -s
dig example.com
dig @8.8.8.8 example.com
```

### **장애 대응시 최소 암기 세트**
```
systemctl status myapp # 서비스 유닛 상태 확인
journalctl -u myapp --since "10 minutes ago" #

df -h
du -xhd1 /var | sort -h
lsof | grep deleted

free -h
vmstat 1
htop

ss -tulpen
ss -s
dig example.com

dmesg -T | grep -i -E "oom|killed|error"
```

장애 순서
```
1. 프로세스가 살아 있는가?
2. 로그에 에러가 있는가?
3. CPU / memory / disk / network 중 어디가 막혔는가?
4. Grafana 지표와 OS 증거가 일치하는가?
5. 조치 후 지표가 회복됐는가?
```

## 10. Incident 1: 서비스 지연 모의 대응
### 배경
SetLog 사용자는 친구들과 같은 방에 들어가 매 시간 짧은 clip을 찍고, 서로의 현재 시간대 기록이 방 feed에 채워지는지 확인한다. 평소에는 정각에 찍은 clip이 곧바로 친구 방 timeline에 붙지만, 이번 실습에서는 "방금 찍은 2초 영상이 계속 업로드 중으로 남는다", "친구가 찍었다고 했는데 내 feed에는 이전 시간대만 보인다"는 신고가 들어온다.

### 문제 상황
진행자가 서비스 지연 Incident를 시작한다. 참가자는 원인을 바로 단정하지 말고, 사용자가 실제로 기다리는 흐름과 page signal을 먼저 확인한다.

- latency가 언제부터 올랐는가?
- 현재 시간대 clip 업로드, 친구 방 feed 확인, daily vlog render 중 어떤 여정이 영향을 받는가?
- error가 주된 증상인가, latency가 주된 증상인가?
- DB pool, CPU, dependency, 로그 중 어떤 보조 신호가 같은 시점에 움직이는가?
- 완화 뒤 같은 사용자 여정이 다시 baseline에 가까워졌는가?

### Incident 시작

진행자가 공용 실습 환경에서 Incident를 시작했다면 아래 명령은 실행하지 않는다. 로컬에서 혼자 실습할 때만 한 줄로 시작한다. 이 스크립트는 fault를 한 번 주입한 뒤 종료한다. 관측 traffic은 Compose의 `baseline-traffic` 서비스가 계속 만든다.

| 환경 | 명령 |
|---|---|
| macOS, Linux, WSL, Git Bash | `sh scripts/incident-1-start.sh` |
| PowerShell | `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\incident-1-start.ps1` |
| cmd | `scripts\incident-1-start.cmd` |

실행이 끝나면 "Incident 1 is active" 메시지를 확인하고 Grafana에서 표를 작성한다.

### Grafana / PromQL 관찰

Grafana의 `SRE301 Golden Signals Lab` 대시보드에서 시간 범위를 최근 15분으로 맞춘다.

| 구분 | 볼 것 | 질문 |
|---|---|---|
| 페이지 조건 | `Latency p95` | 사용자 요청이 baseline보다 오래 기다리는가? |
| 영향 범위 | `Latency by API` | upload, feed, render 중 어디가 느린가? |
| Error | `Traffic and Status` | 실패율이 주된 증상인가, 대기 시간이 주된 증상인가? |
| 보조 신호 | `DB Pool Saturation` | latency 상승과 같은 시점에 pending이 생겼는가? |
| 보조 신호 | `Node CPU Saturation` | CPU가 latency를 설명할 만큼 같이 움직이는가? |

Prometheus에서 직접 확인한다.

```promql
histogram_quantile(
  0.95,
  sum(rate(http_server_requests_seconds_bucket{job="setlog", uri=~"/api/.*"}[5m])) by (le, uri)
)
```

```promql
hikaricp_connections_pending{job="setlog"}
```

### Linux / Docker 확인

Grafana에서 세운 가설을 컨테이너와 OS 도구로 확인한다.

```bash
docker compose ps
docker compose logs setlog --tail=100
docker stats --no-stream
docker compose exec mysql mysql -uroot -proot -e "SHOW FULL PROCESSLIST"
docker compose exec mysql mysql -N -uroot -proot -e "SELECT ID, INFO FROM information_schema.PROCESSLIST WHERE INFO LIKE '%SRE301_I1_DB_POOL_HOLDER%'"
```

확인할 것:

- setlog와 mysql 컨테이너는 살아 있는가?
- MySQL 자체 장애라기보다 앱 요청이 DB 연결을 기다리는 모양인가?
- CPU가 높지 않은데도 latency가 높은가?
- service log에 connection timeout, pending, slow request 흔적이 있는가?

### 완화

아래 제약을 지키면서 실행할 조치와 검증 방법을 먼저 작성한다.

제약:

- 진행자용 reset은 사용하지 않는다.
- baseline traffic은 계속 흐르는 상태에서 조치한다.
- dependency가 정상이라면 dependency부터 재기동하지 않는다.
- 한 번에 하나의 조치만 실행하고, 조치 전후를 같은 증상 지표로 비교한다.

아래 칸에 본인이 실행할 명령을 먼저 적고, 조별로 합의한 뒤 실행한다.

```text
1차 완화 대상:
1차 완화 명령:
상태 확인 명령:
회복 검증 지표:
```

정답 명령은 별도 진행자 문서에만 둔다. 이 실습에서는 멘티가 직접 선택하고 실행한 완화 조치로 회복을 만들어야 한다.

### 실습 문제

아래 표를 직접 채운다. root cause를 바로 맞히려 하지 말고, 사용자 증상에서 시작한다.

| 항목                                 | 답                                             |
|------------------------------------|-----------------------------------------------|
| 사용자 증상 (Symptoms)                  |                                               |
| 영향 범위와 심각도 (Incident Impact)       |                                               |
| 페이지 시그널 (Alert / Page Signal)      | HighLatency<br>p95 API latency is above 800ms |
| 함께 움직인 Diagnostic 신호               |                                               |
| Linux / Docker에서 확인한 증거 (Evidence) |                                               |
| 원인 가설 (Hypothesis)                 |                                               |
| 완화 조치 (Mitigation)                 |                                               |
| 회복 검증 (Recovery Validation)        |                                               |
| 후속 대응 (Follow-up Action Items)     |                                               |

### 완료 기준

- 사용자가 기다리는 현상을 사용자 증상으로, p95 latency를 page signal로 기록했다.
- 영향 API와 시작 시점을 적었다.
- DB pool, CPU, dependency, 로그를 diagnostic 신호로 구분했다.
- reset 명령이 아니라 직접 선택한 완화 조치 전후의 p95 latency와 사용자 여정을 비교했다.

## 11. Incident 2: 에러 증가 모의 대응
![](image%2028.png)<!-- {"width":324} -->

### 배경
SetLog의 핵심 여정은 친구 방을 만들거나 초대 코드로 합류하고, 시간대마다 짧은 clip을 찍어 방 timeline에 채우고, 하루가 끝나면 daily vlog로 묶어 보는 것이다. 사용자는 내부 dependency 이름을 알지 못한다. 사용자가 경험하는 것은 "친구 방을 새로 만들 수 없다", "방에 들어왔는데 지금 시간대 기록을 올릴 수 없다", "찍은 clip이 실패해서 친구들과 같은 하루를 채우지 못한다"는 증상이다.

### 문제 상황

진행자가 에러 증가 Incident를 시작한다. 참가자는 Grafana와 Prometheus를 보고 친구 방 시작, 현재 시간대 clip 업로드, daily vlog 생성 요청 중 어디가 실제로 깨지는지 판단한다.

- 5xx ratio가 언제부터 얼마나 늘었는가?
- traffic 자체가 줄어든 것인가, 요청은 들어오는데 실패하는 것인가?
- 친구 방 생성, 현재 시간대 clip 업로드, daily vlog 생성 요청 중 어떤 사용자 여정이 실패하는가?
- dependency, 로그, 컨테이너 상태 중 어떤 보조 신호가 같은 시점에 흔들리는가?
- 복구 후 실제 API 요청이 다시 성공하는가?

### Incident 시작

진행자가 공용 실습 환경에서 Incident를 시작했다면 아래 명령은 실행하지 않는다. 로컬에서 혼자 실습할 때만 한 줄로 시작한다. 이 스크립트는 fault를 한 번 주입한 뒤 종료한다. 관측 traffic은 Compose의 `baseline-traffic` 서비스가 계속 만든다.

| 환경 | 명령 |
|---|---|
| macOS, Linux, WSL, Git Bash | `sh scripts/incident-2-start.sh` |
| PowerShell | `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\incident-2-start.ps1` |
| cmd | `scripts\incident-2-start.cmd` |

실행이 끝나면 "Incident 2 is active" 메시지를 확인하고 Grafana에서 표를 작성한다.

### Grafana / PromQL 관찰

Grafana에서 먼저 `Traffic and Status`를 본다. 실패가 늘었는지, traffic이 끊긴 것인지 구분한다.

| 구분 | 볼 것 | 질문 |
|---|---|---|
| 페이지 조건 | `Traffic and Status` | 5xx ratio가 threshold를 넘었는가? |
| 영향 범위 | `Latency by API` / status by API | 어떤 API가 실패하는가? |
| 도메인 증상 | `SetLog Clip Uploads` | upload success rate가 떨어지는가? |
| 보조 신호 | Prometheus target | dependency exporter가 down인가? |
| 보조 신호 | service logs | DB connection/write failure가 보이는가? |

Prometheus에서 직접 확인한다.

```promql
sum(rate(http_server_requests_seconds_count{job="setlog", uri=~"/api/.*", status=~"5.."}[5m]))
/
clamp_min(sum(rate(http_server_requests_seconds_count{job="setlog", uri=~"/api/.*"}[5m])), 0.001)
```

```promql
sum(rate(setlog_clip_uploads_total{job="setlog", result="success"}[5m]))
/
clamp_min(sum(rate(setlog_clip_uploads_total{job="setlog"}[5m])), 0.001)
```

다음 쿼리는 원인 후보를 좁히는 보조 신호다. 이것만으로 페이지하지 않는다.

```promql
up{job="mysqld-exporter"}
```

### Linux / Docker 확인

```bash
docker compose ps
docker compose logs setlog --tail=100
docker compose logs mysql --tail=100
docker compose logs mysqld-exporter --tail=100
```

확인할 것:

- setlog는 살아 있지만 사용자 요청이 실패하는가?
- 실패가 특정 API에만 국한되는가, 핵심 여정 전체로 퍼지는가?
- dependency 상태 변화와 5xx ratio 상승 시점이 일치하는가?
- 로그가 Prometheus에서 본 증상과 같은 방향을 가리키는가?

### 완화

아래 제약을 지키면서 실행할 조치와 검증 방법을 먼저 작성한다.

제약:

- 진행자용 reset은 사용하지 않는다.
- 앱을 먼저 재시작하기 전에 dependency 상태를 확인한다.
- 내려간 component만 복구하고, 앱 조치는 5xx가 계속될 때만 추가한다.
- 조치 전후를 같은 증상 지표로 비교한다.

아래 칸에 본인이 실행할 명령을 먼저 적고, 조별로 합의한 뒤 실행한다.

```text
dependency 복구 명령:
dependency 상태 확인 명령:
조건부 앱 조치 명령:
회복 검증 지표:
```

정답 명령은 별도 진행자 문서에만 둔다. 이 실습에서는 멘티가 직접 선택하고 실행한 완화 조치로 회복을 만들어야 한다.

### 실습 문제
아래 표를 직접 채운다. 원인 후보를 쓰기 전에, 사용자가 실제로 겪은 실패를 먼저 쓴다.
| 항목 | 답 |
|---|---|
| 사용자 증상 (Symptoms) |  |
| 영향 범위와 심각도 (Incident Impact) |  |
| 페이지 시그널 (Alert / Page Signal) |  |
| 함께 움직인 Diagnostic 신호 |  |
| Linux / Docker에서 확인한 증거 (Evidence) |  |
| 원인 가설 (Hypothesis) |  |
| 완화 조치 (Mitigation) |  |
| 회복 검증 (Recovery Validation) |  |
| 후속 대응 (Follow-up Action Items) |  |

### 완료 기준

- 5xx ratio를 count가 아니라 비율로 계산했다.
- 영향 API와 사용자-visible 증상을 기록했다.
- dependency down 신호를 페이지 조건이 아니라 diagnostic 신호로 구분했다.
- 직접 dependency를 복구한 뒤 5xx ratio와 도메인 성공률을 다시 확인했다.

## 12. Incident 3: 디스크 초과 모의 대응
![](image%2029.png)<!-- {"width":372} -->
### 배경

SetLog의 daily vlog render는 친구들이 하루 동안 시간대별로 채운 짧은 clip을 하나의 공유 가능한 브이로그로 묶는 핵심 여정이다. render 과정에서 임시 debug log나 산출물이 `/tmp`에 계속 남으면, 앱은 살아 있어도 새로운 daily vlog 결과물을 안정적으로 만들지 못할 수 있다.

사용자가 경험하는 것은 파일 시스템 상태가 아니다. 사용자가 보는 증상은 "하루치 기록을 브이로그로 만들려고 했는데 실패한다", "공유할 결과물이 준비되지 않는다", "다시 눌러도 render 요청이 실패한다"이다.

### 문제 상황

진행자가 디스크 초과 Incident를 시작한다. 참가자는 disk 그래프만 보고 바로 원인 확정하지 않고, daily vlog render 실패가 실제 사용자-visible 증상인지 먼저 판단한다.

- 사용자가 직접 겪는 증상은 무엇인가?
- render job 실패율이 언제부터 얼마나 증가했는가?
- 전체 API가 실패하는가, render 여정에 집중되어 있는가?
- disk 관련 diagnostic 신호가 render 실패와 같은 시점에 움직이는가?
- 완화 뒤 render 요청과 디스크 증거가 함께 회복되는가?

### Incident 시작

진행자가 공용 실습 환경에서 Incident를 시작했다면 아래 명령은 실행하지 않는다. 로컬에서 혼자 실습할 때만 한 줄로 시작한다. 이 스크립트는 fault를 한 번 주입한 뒤 종료한다. 관측 traffic은 Compose의 `baseline-traffic` 서비스가 계속 만든다.

| 환경 | 명령 |
|---|---|
| macOS, Linux, WSL, Git Bash | `sh scripts/incident-3-start.sh` |
| PowerShell | `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\incident-3-start.ps1` |
| cmd | `scripts\incident-3-start.cmd` |

실행이 끝나면 "Incident 3 is active" 메시지를 확인하고 Grafana에서 표를 작성한다.

### Grafana / PromQL 관찰

| 구분 | 패널 | 질문 |
|---|---|---|
| 페이지 조건 | `Vlog Render Queue, Jobs, and Debug Log` | render failure 비율이 증가했는가? |
| 페이지 조건 | `Traffic and Status` | `/api/render-jobs` 5xx가 사용자 실패로 보이는가? |
| 영향 범위 | `Latency by API` / status by API | 실패가 render에 집중되는가, 전체 API로 퍼지는가? |
| 보조 신호 | `Vlog Render Queue, Jobs, and Debug Log` | render debug log bytes가 같은 시점에 증가했는가? |
| 보조 신호 | `Node CPU Saturation` | CPU가 주된 설명이 아닌가? |

Prometheus에서 직접 확인한다.

```promql
sum(rate(setlog_vlog_render_jobs_total{job="setlog", result="failure"}[5m]))
/
clamp_min(sum(rate(setlog_vlog_render_jobs_total{job="setlog"}[5m])), 0.001)
```

```promql
setlog_render_debug_log_bytes{job="setlog"}
```

다음 쿼리는 영향 API를 확인하는 보조 신호다.

```promql
sum(rate(http_server_requests_seconds_count{job="setlog", uri=~"/api/.*", status=~"5.."}[5m])) by (uri)
```

### Linux / Docker 확인

```bash
docker compose exec setlog df -h
docker compose exec setlog du -sh /tmp
docker compose exec setlog sh -c "ls -lh /tmp/sre301-render-debug.log 2>/dev/null || true"
docker stats --no-stream
docker compose logs setlog --tail=100
```

확인할 것:

- `/tmp` 사용량이 증가했는가?
- `/tmp/sre301-render-debug.log` 파일이 실제로 보이는가?
- render 실패 로그가 disk 관련 증거와 같은 시점인가?
- CPU가 render 실패를 설명할 만큼 높지 않은가?

### 완화
아래 제약을 지키면서 실행할 조치와 검증 방법을 먼저 작성한다.

제약:
- 진행자용 reset은 사용하지 않는다.
- baseline traffic은 계속 흐르는 상태에서 조치한다.
- 디스크 정리와 앱 재시작을 한 번에 섞지 않는다.
- 조치 하나마다 어떤 지표가 회복됐는지 기록한다.

아래 칸에 본인이 실행할 명령을 먼저 적고, 조별로 합의한 뒤 실행한다.
```text
1차 완화 대상:
1차 완화 명령:
1차 검증 지표:

2차 완화 대상:
2차 완화 명령:
2차 검증 지표:

회복 검증 지표:
```

정답 명령은 별도 진행자 문서에만 둔다. 이 실습에서는 멘티가 직접 선택하고 실행한 완화 조치로 회복을 만들어야 한다.

### 실습 문제

아래 표를 직접 채운다. 원인 후보를 쓰기 전에, 사용자가 실제로 겪은 실패와 영향부터 쓴다.

| 항목 | 답 |
|---|---|
| 사용자 증상 (Symptoms) |  |
| 영향 범위와 심각도 (Incident Impact) |  |
| 페이지 시그널 (Alert / Page Signal) |  |
| 함께 움직인 Diagnostic 신호 |  |
| Linux / Docker에서 확인한 증거 (Evidence) |  |
| 원인 가설 (Hypothesis) |  |
| 완화 조치 (Mitigation) |  |
| 회복 검증 (Recovery Validation) |  |
| 후속 대응 (Follow-up Action Items) |  |

### 완료 기준
- daily vlog 생성 실패를 사용자 증상으로, render failure ratio를 page signal로 기록했다.
- disk/debug log 지표를 page signal이 아니라 diagnostic 신호로 구분했다.
- `df`, `du`, `ls`로 Grafana 가설을 확인했다.
- 직접 실행한 완화 조치 후 render failure ratio와 debug log bytes를 다시 비교했다.

## 13. Incident 4: CPU Saturation 모의 대응

> 그림: CPU high는 diagnostic 신호이고, page 조건은 render latency와 queue backlog다.
![](sre301-incident4-cpu-render.png)<!-- {"width":760} -->
> 훈련할 판단: CPU high를 단독 page signal로 보지 않고, render latency와 backlog를 먼저 확인한다.
> 공통 원칙: root cause를 바로 맞히려 하지 말고, 사용자 증상과 page signal을 먼저 고정한 뒤 Linux/Docker 증거로 가설을 좁힌다.

### 배경
SetLog 사용자는 하루치 clip을 daily vlog로 묶고, 결과물을 바로 저장하거나 친구에게 공유한다. render worker가 CPU를 과도하게 사용하면 앱은 살아 있어도 render 요청과 feed 확인이 느려질 수 있다. 사용자가 경험하는 것은 "render를 눌렀는데 계속 기다린다", "앱 반응이 둔하다", "결과물이 늦게 준비된다"는 증상이다.

### 문제 상황

진행자가 CPU 사용률 상승과 render 지연이 함께 보이는 Incident를 시작한다. 참가자는 CPU 그래프만 보고 page하지 않고, 사용자-visible latency와 render backlog가 실제로 움직이는지 먼저 판단한다.

- 사용자가 직접 겪는 증상은 무엇인가?
- CPU 상승과 render latency 상승 시점이 일치하는가?
- disk/debug log 증가 없이도 CPU가 지연을 설명하는가?
- render queue depth가 backlog를 실제로 보여주는가?
- 완화 뒤 CPU, render latency, render queue depth가 어떻게 회복되는가?

### Incident 시작

진행자가 공용 실습 환경에서 Incident를 시작했다면 아래 명령은 실행하지 않는다. 로컬에서 혼자 실습할 때만 한 줄로 시작한다. 이 스크립트는 fault를 한 번 주입한 뒤 종료한다. 관측 traffic은 Compose의 `baseline-traffic` 서비스가 계속 만든다.

| 환경 | 명령 |
|---|---|
| macOS, Linux, WSL, Git Bash | `sh scripts/incident-4-start.sh` |
| PowerShell | `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\incident-4-start.ps1` |
| cmd | `scripts\incident-4-start.cmd` |

실행이 끝나면 "Incident 4 is active" 메시지를 확인하고 Grafana에서 표를 작성한다.

### Grafana / PromQL 관찰

| 구분 | 패널 | 질문 |
|---|---|---|
| 페이지 조건 | `Latency p95` / render latency | 사용자가 기다리는 시간이 늘었는가? |
| 페이지 조건 | `Vlog Render Queue, Jobs, and Debug Log` | render queue depth가 올라가는가? |
| 도메인 증상 | `Vlog Render Queue, Jobs, and Debug Log` | render failure보다 render 지연과 backlog가 주된 증상인가? |
| 보조 신호 | `Node CPU Saturation` | CPU 상승이 같은 시점인가? |
| 보조 신호 | `Vlog Render Queue, Jobs, and Debug Log` | debug log bytes는 주된 설명이 아닌가? |

Prometheus에서 직접 확인한다.

```promql
histogram_quantile(
  0.95,
  sum(rate(http_server_requests_seconds_bucket{job="setlog", uri="/api/render-jobs"}[5m])) by (le)
)
```

```promql
setlog_vlog_render_queue_depth{job="setlog"}
```

다음 쿼리는 원인 후보를 좁히는 보조 신호다.

```promql
1 - avg(rate(node_cpu_seconds_total{mode="idle"}[2m]))
```

### Linux / Docker 확인

```bash
docker stats --no-stream
docker compose logs setlog --tail=100
docker compose exec setlog df -h
docker compose exec setlog sh -c "ls -lh /tmp/sre301-render-debug.log 2>/dev/null || true"
```

확인할 것:

- CPU 사용률 상승이 setlog와 관련 있는가?
- setlog는 살아 있지만 사용자 요청이 느린가?
- disk/debug log 증거는 큰 변화가 없는가?
- render queue depth와 render latency가 사용자 증상과 더 직접적으로 연결되는가?

### 완화

아래 제약을 지키면서 실행할 조치와 검증 방법을 먼저 작성한다.

제약:

- 진행자용 reset은 사용하지 않는다.
- baseline traffic은 계속 흐르는 상태에서 조치한다.
- disk cleanup으로 CPU 문제를 해결하려고 하지 않는다.
- 조치 전후를 같은 증상 지표로 비교한다.

아래 칸에 본인이 실행할 명령을 먼저 적고, 조별로 합의한 뒤 실행한다.

```text
1차 완화 대상:
1차 완화 명령:
1차 검증 지표:

회복 검증 지표:
```

정답 명령은 별도 진행자 문서에만 둔다. 이 실습에서는 멘티가 직접 선택하고 실행한 완화 조치로 회복을 만들어야 한다.

### 실습 문제
아래 표를 직접 채운다. 원인 후보를 쓰기 전에, 사용자가 실제로 겪은 지연과 영향부터 쓴다.
| 항목                                 | 답   |
|------------------------------------|-----|
| 사용자 증상 (Symptoms)                  | x   |
| 영향 범위와 심각도 (Incident Impact)       |     |
| 페이지 시그널 (Alert / Page Signal)      |     |
| 함께 움직인 Diagnostic 신호               |     |
| Linux / Docker에서 확인한 증거 (Evidence) |     |
| 원인 가설 (Hypothesis)                 |     |
| 완화 조치 (Mitigation)                 |     |
| 회복 검증 (Recovery Validation)        |     |
| 후속 대응 (Follow-up Action Items)     |     |

### 완료 기준
- render/API 지연을 사용자 증상으로, latency와 queue depth를 page signal로 기록했다.
- CPU high를 page signal이 아니라 diagnostic 신호로 구분했다.
- `docker stats`로 Grafana 가설을 확인했다.
- 직접 실행한 완화 조치 후 CPU, render queue, p95 latency를 다시 비교했다.

## 14. 장애 대응 Mini Postmortem 및 Runbook 작성
> [Google SRE - Blameless Postmortem for System Resilience](https://sre.google/sre-book/postmortem-culture/)
> example1: [Postmortem of database outage of January 31](https://about.gitlab.com/blog/postmortem-of-database-outage-of-january-31/)
> example2: [Details of the Cloudflare outage on July 2, 2019](https://blog.cloudflare.com/details-of-the-cloudflare-outage-on-july-2-2019/)
> example3: https://tech.kakaoent.com/tech/ifkakao-2021-postmortem/
> Kakao: [우리가 부족했던 이유](https://www.kakaocorp.com/page/detail/9902)
> Kakao: [당연한 일상을 지키는  카카오의 일상](https://www.kakaocorp.com/page/detail/10619), [안전성 보고서](https://t1.kakaocdn.net/kakaocorp/kakaocorp/admin/promise/report/KakaoReliabilityReport_online.pdf)
> Kakao Interview Video: [1cm² 속 보이지 않는 카카오의 노력 🔍 | 365일 24시간 카톡을 지키는 사람들](https://www.youtube.com/watch?v=ro_6M5qc604)
마지막 10~15분에는 Incident 하나를 골라 미니 포스트모텀을 작성합니다. 
목표는 완벽한 보고서가 아니라, 
대응 과정에서 배운 내용을 다음 장애 대응에 재사용 가능한 형태로 남기는 것입니다.

작성 범위:
- Summary
- Impact
- Detection
- Resolution
- Follow-up Action Items

Root cause를 길게 쓰려고 시간을 다 쓰지 않습니다. 오늘의 핵심은 사용자 영향, 감지 신호, 실행한 완화 조치, 회복 검증, 재발 방지 액션을 분리해서 쓰는 것입니다.

```markdown
# [서비스명] 장애 미니 포스트모텀

Date:
Authors:
Incident:

## Summary
YYYY-MM-DD HH:mm부터 HH:mm까지 [서비스/기능]에서 [사용자-visible 증상]이 발생했다.
이로 인해 [사용자 영향]이 있었고, [완화/복구 조치]를 통해 정상화했다.

## Impact
- 장애 시간:
- 영향받은 사용자/방/요청:
- 영향받은 기능:
- 실패 요청 수 / 비율:
- 사용자 관점의 영향:

## Detection
- 최초 감지 시각:
- Page signal:
- Diagnostic signal:
- 더 빨리 발견할 수 있었던 신호:

## Resolution
- 즉시 완화:
- 회복 검증 지표:
- 정상화 판단 기준:

## Timeline
- 2026.06.18 04:00:00 X 발견
- 2026.06.18 04:02:00 Y 조치
- 2026.06.18 04:03:00 Z 완화

## Follow-up Action Items
| Action Item | Type | Owner | Status |
|---|---|---|---|
| [런북 업데이트] | mitigate |  | TODO |
| [재발 방지 수정] | prevent |  | TODO |
| [회귀 테스트/장애 훈련 추가] | process |  | TODO |
```

Type:
- mitigate: 같은 장애가 나도 피해를 줄임
- prevent: 같은 원인의 재발을 막음
- process: 운영/대응 절차 개선
- other: 정책, 조직, 제품 판단

Runbook은 장애 중에 다음 행동을 빠르게 고르기 위한 절차서입니다. 좋은 Runbook은 “무엇을 보면 되는지”, “어떤 명령을 실행하는지”, “언제 멈추고 에스컬레이션하는지”, “어떤 지표로 회복을 확인하는지”를 짧게 적습니다.

### Runbook 예시: SetLog API / Render 장애 초기 대응
> [Awesome Runbook - 런북 큐레이션 리스트 | GeekNews](https://news.hada.io/topic?id=10486)

> 목적: SetLog에서 사용자가 upload, feed, daily vlog render 지연이나 실패를 겪을 때 10분 안에 영향 범위를 좁히고 1차 완화 조치를 선택한다.

적용 조건:
- `Latency p95`, `Traffic and Status`, `Vlog Render Queue, Jobs, and Debug Log` 중 하나 이상이 baseline보다 나빠졌다.
- 사용자 신고가 있거나 baseline traffic에서 upload/feed/render 여정이 실패 또는 지연된다.

#### 1. 사용자 증상과 page signal을 먼저 고정한다
- 사용자가 기다리는가?
  - 먼저 볼 것: `Latency p95`, `Latency by API`
  - 기록할 내용: 느린 API, 시작 시점, p95 변화
- 사용자가 실패를 보는가?
  - 먼저 볼 것: `Traffic and Status`, 5xx ratio
  - 기록할 내용: 실패 API, 실패 비율, 영향 여정
- render가 밀리는가?
  - 먼저 볼 것: `Vlog Render Queue, Jobs, and Debug Log`
  - 기록할 내용: failure ratio, queue depth, debug log bytes

#### 2. Diagnostic signal로 원인 후보를 좁힌다

#### upload/feed가 느림
확인할 diagnostic signal:
- `DB Pool Saturation`
- `hikaricp_connections_pending`

Linux / Docker 확인:
```bash
docker compose logs setlog --tail=100
docker compose exec mysql mysql -uroot -proot -e "SHOW FULL PROCESSLIST"
docker compose exec mysql mysql -N -uroot -proot -e "SELECT ID, INFO FROM information_schema.PROCESSLIST WHERE INFO LIKE '%SRE301_I1_DB_POOL_HOLDER%'"
```

#### 5xx가 증가함
확인할 diagnostic signal:
- Prometheus target
- `up{job="mysqld-exporter"}`

Linux / Docker 확인:
```bash
docker compose ps
docker compose logs mysql --tail=100
docker compose logs mysqld-exporter --tail=100
```

#### render가 실패함
확인할 diagnostic signal:
- `setlog_render_debug_log_bytes`
- render failure ratio

Linux / Docker 확인:
```bash
docker compose exec setlog df -h
docker compose exec setlog du -sh /tmp
docker compose exec setlog sh -c 'ls -lh /tmp/sre301-render-debug.log 2>/dev/null || true'
```

#### render가 느리고 backlog가 쌓임
확인할 diagnostic signal:
- CPU saturation
- render queue depth

Linux / Docker 확인:
```bash
docker stats --no-stream
docker compose logs setlog --tail=100
```

#### 3. 한 번에 하나의 완화 조치만 실행한다

#### DB pool pending이 높고 MySQL은 살아 있음
1차 완화 조치:
```bash
docker compose exec mysql mysql -N -uroot -proot -e "SELECT ID, INFO FROM information_schema.PROCESSLIST WHERE INFO LIKE '%SRE301_I1_DB_POOL_HOLDER%'"
docker compose exec mysql mysql -uroot -proot -e "KILL <id>;"
```
하지 말 것: MySQL부터 재시작하지 않는다. `docker compose restart setlog`는 SQL로 점유 세션을 제거할 수 없을 때의 emergency fallback으로만 둔다.

#### MySQL 또는 mysqld-exporter가 내려감
1차 완화 조치:
```bash
docker compose up -d mysql mysqld-exporter
```
하지 말 것: 앱부터 재시작하지 않는다.

#### `/tmp/sre301-render-debug.log`가 커져 render가 실패함
1차 완화 조치:
```bash
docker compose exec setlog rm -f /tmp/sre301-render-debug.log
```
하지 말 것: cleanup과 app restart를 한 번에 섞지 않는다.

#### CPU-bound render worker로 latency/backlog가 증가함
1차 완화 조치:
```bash
docker compose restart setlog
docker compose restart setlog-netem
```
`setlog-netem`은 `setlog` network namespace에 붙은 sidecar이므로 `setlog` 재시작 뒤 함께 재시작해 Compose health를 회복한다. 하지 말 것: disk cleanup으로 CPU 문제를 해결하려고 하지 않는다.

#### 4. 같은 지표와 같은 사용자 여정으로 회복을 검증한다
- Latency 장애: 상시 baseline traffic에서 `hikaricp_connections_pending`이 0 근처로 돌아오고 upload/feed p95가 baseline에 가까워졌는가?
- 5xx 장애: `up{job="mysqld-exporter"}`가 1이고 room 생성과 clip upload가 성공하는가?
- Render 실패: `setlog_render_debug_log_bytes`가 내려가고 render failure ratio가 회복됐는가?
- CPU saturation: CPU, render latency, render queue depth가 baseline에 가까워졌는가?

#### 5. 멈추고 에스컬레이션할 기준을 정한다
- 1차 완화 후에도 page signal이 5분 이상 회복되지 않는다.
- 사용자가 보는 실패가 upload/feed/render 전체로 확산된다.
- 데이터 손실 가능성이 있거나 DB write 실패가 계속된다.
- 어떤 조치를 실행했는지와 그 결과를 설명할 수 없을 만큼 변경이 섞였다.

에스컬레이션할 때는 “원인 추측”보다 아래 정보를 먼저 공유한다.
- 사용자 증상
- 시작 시점
- page signal
- diagnostic signal
- 실행한 조치
- 조치 후 회복 여부


## 마무리
> 마무리 퀴즈: [SRE 퀴즈](https://notebooklm.google.com/notebook/9f5fc264-4715-4bbd-ab24-55b9c84205b6/artifact/69a67277-c169-46ff-9651-3fafeaefedd2?utm_source=nlm_web_share&utm_medium=google_oo&utm_campaign=art_share_1&utm_content=&utm_smc=nlm_web_share_google_oo_art_share_1_)
- Golden Signal
- SLO
- 프로젝트 삼각형 (Project Triangle)
- CAP Theorem
- Time, Scale, Trade-Offs
- 리틀의 법칙

## 읽어볼만한 레퍼런스 정리
- [원문: My Philosophy on Alerting](https://docs.google.com/document/d/199PqyG3UsyXlwieHaqbGiWVa8eMWi8zzAn0YfcApr8Q/edit?tab=t.0#heading=h.fs3knmjt7fjy)
- [De한글 번역: My Philosophy on Alerting](https://docs.google.com/document/d/1-Fnb2ibZ6fQJBA4az_7YHdzidYJHnDhTKcl-reGdu3A/edit?tab=t.0)
- [The USE Method — Brendan Gregg](https://www.brendangregg.com/usemethod.html)
- [The RED Method: How to Instrument Your Services | Grafana Labs](https://grafana.com/blog/the-red-method-how-to-instrument-your-services/)
  - [grafana Slide](https://grafana.com/files/grafanacon_eu_2018/Tom_Wilkie_GrafanaCon_EU_2018.pdf?pg=the-red-method-how-to-instrument-your-services&plcmt=in-text)

---
## 법칙들
- 리틀의 법칙
```
# L -> 상점에 있는 평균 고객 수
# λ(람다) -> 대기열에 있는 사람 수
# W -> 상점에 머무르는 시간
L = λ * W

# 예를 들어, 5시간 동안 300명의 손님이 다녀갔고 인당 평균 식사 시간이 10분일 경우 식당 내의 평균 손님 수는 얼마일까?
# Answer: λ=300명/5시간=60명/시간, W=1/6시간, L=60*1/6=10명
```
즉 응답시간을 낮추는 것이 시스템 성능 향상의 핵심 지표.

- 스토리지
```
# Thread → I/O 부하를 일으키는 주체
# Latency(지연시간) →  스토리지의 응답시간
# IOPS(Input Output Operations per Second) → 초당 입출력 오퍼레이션 건수로 스토리지의 처리량
IOPS = Threads / Latency
```
- TPS
```
# TPS(초당 트랜잭션)와 Response
TPS = Active users / Response time
Response time = Active users / TPS
```
![](image%2022.png)
