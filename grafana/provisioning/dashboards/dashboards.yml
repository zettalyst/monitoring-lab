apiVersion: 1

providers:
  - name: SetLog
    type: file
    allowUiUpdates: true
    folder: SetLog
    options:
      path: /var/lib/grafana/dashboards/setlog

  - name: SetLog Support
    type: file
    allowUiUpdates: true
    folder: SetLog Support
    options:
      path: /var/lib/grafana/dashboards/setlog-support
